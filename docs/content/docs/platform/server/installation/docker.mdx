---
title: Docker
description: Install Agin CI server using Docker
---
import { IconBrandDocker, IconPackages } from '@tabler/icons-react';

<Callout>
Before you begin the installation, make sure to read the [installation prerequisites](/platform/server/installation) section.
</Callout>

## Install using Docker

This guide will use Docker Compose to install the Agin CI server along with a MongoDB database, Valkey, and Pulsar. You can also use the official Docker image directly, but using Docker Compose is recommended for easier management.